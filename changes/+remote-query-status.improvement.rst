Blocking remote query calls now show timestamped job status transitions while
waiting, and update in-place when running inside a Jupyter notebook. Pass
``show_status=False`` to keep the previous silent behavior.
