[package]
name = "opencosmo"
version = "1.2.4"
edition = "2021"

[lib]
name = "_lib"
# "cdylib" is necessary to produce a shared library for Python to import from.
crate-type = ["cdylib"]

[dependencies]
numpy = "0.28"
pyo3 = "0.28.2"

